{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
  <h1>Articles</h1>

  {% if posts is empty %}
    <p>Aucun article pour le moment.</p>
  {% else %}
    <ul>
      {% for post in posts %}
        <li><a href="{{ path('article_show', {id: post.id}) }}">{{ post.title }}</a> — {{ post.publishedAt ? post.publishedAt|date('d/m/Y H:i') : '' }}</li>
      {% endfor %}
    </ul>
  {% endif %}
{% endblock %}